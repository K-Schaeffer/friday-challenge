{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "preview": "nx graph"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "lerna": "^6.4.1",
    "husky": "^8.0.0"
  },
  "engines": {
    "node": "18.x",
    "yarn": "1.x"
  }
}
